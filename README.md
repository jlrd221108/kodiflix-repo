# Kodiflix Repository

Kodi add-on repository for Kodiflix.

Install the repository zip, then install Kodiflix from the repository.

Repository metadata is generated using the standard Kodi repository layout from `drinfernoo/repository.example`.
